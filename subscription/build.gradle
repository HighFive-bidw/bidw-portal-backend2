dependencies {
    implementation project(':common')
    
    // REST 클라이언트
    implementation 'org.springframework.boot:spring-boot-starter-webflux'
}
bootJar {
    archiveFileName = "subscription-service.jar"
}
